{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "postinstall": "echo '--- CURRENT DIRECTORY ---' && pwd && echo '--- DIRECTORY CONTENTS ---' && ls -la && echo '--- PARENT DIRECTORY CONTENTS ---' && ls -la .. && echo '--- NPM VERSION ---' && npm --version && echo '--- NODE VERSION ---' && node --version",
    "start": "node server.mjs",
    "dev": "nodemon server.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@solana/spl-token-registry": "^0.2.4574",
    "@solana/web3.js": "^1.98.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongodb": "^6.12.0",
    "mongoose": "^8.9.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
 }